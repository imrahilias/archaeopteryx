#!/bin/sh

# get primary selection
url=`xclip -out -selection primary`

echo $url

# start in browser
google-chrome-stable "https://translate.google.com/#view=home&op=translate&sl=auto&tl=en&text=$url"
