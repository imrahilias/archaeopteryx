#!/bin/bash
# call a process, return running or not

PROCESS=$1
PIDS=`pgrep $PROCESS`
if [ -z "$PIDS" ]; then
  echo "Process not running." 1>&2
  exit 1
else
echo $PIDS
#  for PID in $PIDS; do
#    echo $PID
#  done
fi
